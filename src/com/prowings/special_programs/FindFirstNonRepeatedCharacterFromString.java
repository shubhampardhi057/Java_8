package com.prowings.special_programs;

public class FindFirstNonRepeatedCharacterFromString {
	
	public static void main(String[] args) {
		
		String input = "programming";
//		
//		Optional<Character> firstNonRepatedChar = input.chars()
//														.mapToObj(c -> (char)c)
//														.collect(LinkedHashMap::new,
//										                        (map, ch) -> map.merge(ch, 1, Integer::sum),
//										                        LinkedHashMap::putAll)
//														.entrySet()
//														.stream()
//														.filter(entry -> entry.getValue() == 1)
//														.map(Map.Entry::getKey)
//														.findFirst();
//		
//		if (firstNonRepatedChar.isPresent())
//		{
//	            System.out.println("First non-repeated character: " + firstNonRepatedChar.get());
//	    } 
//		else 
//		{
//	            System.out.println("No non-repeated characters found.");
//	    }
//											
																
																
	}

}
